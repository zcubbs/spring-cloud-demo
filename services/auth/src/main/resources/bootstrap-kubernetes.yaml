spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
      discovery:
        enabled: true
eureka:
  client:
    enabled: false

management:
  endpoint:
    restart:
      enabled: true
